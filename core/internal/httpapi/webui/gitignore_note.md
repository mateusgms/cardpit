# dist/ staging

`dist/index.html` committed here is a placeholder so `go build` works on a
fresh clone. `make web` replaces the whole `dist/` directory in the working
tree with the real Vite build output (never committed — see `.gitignore` at
the repo root).
